package DesingPatern;

public interface RegrasEstoque {

    //Adiciona Produtos ao estoque

    void adicionarProduto(String nome, double valor, int quantidade);
    //Retira Produtos do estoque

    void retirarProduto(int id);

    //Editar Quantidade do Item no ID
    void editarQuantidade(int id, int quantidade);
    
    //Buscar Item pelo nome do Produto

    void buscarPorNomeDeProduto(String produto);

    //Mostra Total que está no estoque

    double TotalEstoque();

    //Verifica se tem aquele item em estoque

    boolean verificarDisponibilidade(int id,int quantidade);
}
